server {
    listen      80;
    server_name localhost;
    charset     utf-8;
    client_max_body_size 75M;


    location / {
        include uwsgi_params;
        uwsgi_pass unix:///var/run/uwsgi.sock;
    }

    location /static {
      root /home/docker/wtfbot/static;
    }
}
